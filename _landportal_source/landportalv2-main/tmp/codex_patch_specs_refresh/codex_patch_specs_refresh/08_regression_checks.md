# Regression Checks

After patching, verify:
1. title-chain retry/unlink actions still render
2. stored copy / original source actions still work
3. supporting record stack fix is still intact
4. full friendly titles still win over synthetic titles
5. commitment delete/unlink flows still refresh correctly
6. no new TypeScript errors are introduced
