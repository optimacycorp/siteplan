# Regression Checks

After patching, verify:
1. title workflow still works
2. parcel selection still works
3. map does not continuously re-fit on every render
4. designer controls still render
5. adjoining parcel fetch does not break when no neighbors are returned
6. no new TypeScript errors are introduced
