# Regression Checks

After applying the patch, verify:

1. commitment preview still works
2. child document preview still works
3. retry fetch action still renders where it did before
4. unlink action still removes the commitment relationship
5. non-title supporting documents still appear in the Supporting record stack
6. no new TypeScript errors are introduced
7. no tile title becomes blank when no visible chain text exists
